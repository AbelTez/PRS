{
  "name": "ethio-referral-linkage",
  "version": "0.2.0",
  "private": true,
  "description": "Closed-loop referral exchange for Ethiopian health facilities — API (NestJS) + web (React) deployed together.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix web install",
    "build:api": "npm --prefix server run build",
    "build:web": "npm --prefix web run build",
    "build": "npm run build:api && npm run build:web",
    "migrate": "npm --prefix server run migrate",
    "seed": "node server/scripts/seed.js",
    "test:e2e": "node server/scripts/e2e-test.js && node server/scripts/e2e-features.js",
    "dev:api": "npm --prefix server run start:dev",
    "dev:web": "npm --prefix web run dev"
  }
}
